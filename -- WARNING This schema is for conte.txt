-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.igrejas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  codigo_totvs character varying NOT NULL UNIQUE,
  desc_igreja character varying NOT NULL,
  tipo_imovel character varying,
  endereco text,
  bairro character varying,
  municipio character varying,
  estado character varying,
  cep character varying,
  latitude double precision,
  longitude double precision,
  link_google_maps text,
  status character varying DEFAULT 'PENDENTE'::character varying,
  validado_por character varying,
  validado_em timestamp with time zone,
  observacoes text,
  created_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  usuario_validador character varying,
  codigo_totvs_pai character varying,
  porte character varying,
  dirigente_nome character varying,
  dirigente_telefone character varying,
  dirigente_email character varying,
  financeira_nome character varying,
  financeira_telefone character varying,
  financeira_email character varying,
  dirigente_data_posse date,
  qtd_membros integer DEFAULT 0,
  qtd_jovens integer DEFAULT 0,
  tipo_prebenda character varying DEFAULT 'NAO_PREBENDADA'::character varying,
  CONSTRAINT igrejas_pkey PRIMARY KEY (id)
);